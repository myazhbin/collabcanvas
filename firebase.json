{
  "//": "Rules deploy targets only — Vercel does the hosting, so no hosting block here [R5]. The committed rule files are the source of truth from PR 2 on; `firebase deploy` pushes them over whatever is in the console.",
  "firestore": { "rules": "firestore.rules" },
  "database": { "rules": "database.rules.json" },
  "emulators": {
    "auth": { "port": 9099 },
    "firestore": { "port": 8080 },
    "database": { "port": 9000 },
    "ui": { "enabled": false },
    "singleProjectMode": true
  }
}
